# 🔹 Decimal <-> Binary Conversion in C++

# ------------------ THEORY ------------------
# ✅ Decimal to Binary:
# In base-10 (decimal), each digit represents powers of 10.
# In base-2 (binary), each bit represents powers of 2.
# To convert decimal → binary:
#   1. Divide the decimal number by 2 repeatedly.
#   2. Store the remainders (0 or 1).
#   3. Reverse the order of remainders → gives binary.

# Example: Decimal 25
# 25 / 2 = 12 remainder 1
# 12 / 2 = 6  remainder 0
# 6  / 2 = 3  remainder 0
# 3  / 2 = 1  remainder 1
# 1  / 2 = 0  remainder 1
# Binary = reverse(10011) = 11001

# ✅ Binary to Decimal:
# Each binary digit is multiplied by 2 raised to its position (from right to left).
# Example: Binary 11001
# = (1×2⁴) + (1×2³) + (0×2²) + (0×2¹) + (1×2⁰)
# = 16 + 8 + 0 + 0 + 1 = 25

# ------------------ PSEUDO CODE ------------------
# function decimalToBinary(decimal):
#     binary = ""
#     while decimal > 0:
#         remainder = decimal % 2
#         binary = remainder + binary
#         decimal = decimal // 2
#     return binary
#
# function binaryToDecimal(binaryString):
#     decimal = 0
#     power = 0
#     for i from last digit to first:
#         if binaryString[i] == '1':
#             decimal = decimal + 2^power
#         power = power + 1
#     return decimal

int -> 32 bits
long long -> 64 bits


----------------------------------------------------------------------------------
----------------------------------------------------------------------------------
----------------------------------------------------------------------------------
----------------------------------------------------------------------------------
----------------------------------------------------------------------------------

# 🔹 1's Complement and 2's Complement in C++

# ------------------ THEORY ------------------

# ✅ 1's Complement:
# → It is formed by inverting (flipping) all bits of a binary number.
#   0 becomes 1, and 1 becomes 0.
# Example:
#   Binary:  1010
#   1's Comp:0101
#
# 👉 Meaning: 1’s complement represents the negative of a number in older systems.
# However, it has two representations for zero (+0 and -0), which is inefficient.
# So modern computers use 2’s complement.

# ✅ 2's Complement:
# → It is formed by taking the 1’s complement and then adding 1 to it.
# Example:
#   Binary:  1010
#   1's Comp:0101
#   Add 1   :0110  → This is 2’s complement.
#
# 👉 Meaning: 2’s complement represents signed numbers in most systems.
# The leftmost bit (MSB) acts as the sign bit:
#   0 → Positive number
#   1 → Negative number
#
# Example for 4-bit system:
#   +5 → 0101
#   -5 → 2’s complement of 0101 → 1011

# ------------------ PSEUDO CODE ------------------

# function onesComplement(binaryString):
#     for each bit in binaryString:
#         if bit == '0': change to '1'
#         else: change to '0'
#     return new binaryString

# function twosComplement(binaryString):
#     ones = onesComplement(binaryString)
#     carry = 1
#     result = ""
#     for i from last bit to first:
#         if ones[i] == '1' and carry == 1:
#             result = '0' + result
#         elif ones[i] == '0' and carry == 1:
#             result = '1' + result
#             carry = 0
#         else:
#             result = ones[i] + result
#     return result

# ------------------ C++ CODE ------------------
string twosComplement(string bin) {
    string ones = onesComplement(bin);
    string result = ones;
    int carry = 1;
    for (int i = bin.size() - 1; i >= 0; i--) {
        if (ones[i] == '1' && carry == 1)
            result[i] = '0';
        else if (ones[i] == '0' && carry == 1) {
            result[i] = '1';
            carry = 0;
        } else
            result[i] = ones[i];
    }
    return result;
}
# ------------------ SUMMARY ------------------
# 1’s Complement → Flip all bits
# 2’s Complement → Flip all bits + Add 1
# They are used to represent and perform arithmetic on negative numbers in binary.

----------------------------------------------------------------------------------
----------------------------------------------------------------------------------
----------------------------------------------------------------------------------
----------------------------------------------------------------------------------
----------------------------------------------------------------------------------
